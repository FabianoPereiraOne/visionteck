export type postCollectionProps = {
  title: string
  description: string
  themeColor: string
}

export type patchCollectionProps = {
  id: number
  title?: string
  description?: string
  themeColor?: string
}
